# Agent OS Code Owners
#
# Both AI Builders own every file. Either can review any
# PR they didn't author — GitHub blocks self-approval, so
# the author's review never counts toward the required
# Code Owner APPROVE.
#
# On review, Code Owners verify Gemini Code Assist
# feedback was properly addressed — not just the literal
# flagged line, but the same class of issue across the
# touched files.
#
# Paired with branch protection on main requiring
# "Review from Code Owners" — this makes a Code Owner
# APPROVE structurally mandatory before merge.

* @brunny-scout @brunny-kai
